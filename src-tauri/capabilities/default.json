{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "EventFlow desktop capabilities — shell open for Stripe / docs, core window APIs",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
