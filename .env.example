# Optional: enable live OpenAI parsing (AI Planner + invoice Vision)
# Without this key, a rich client-side Czech simulation is used.
VITE_OPENAI_API_KEY=

# Optional: Supabase cloud inventory sync
# Without these keys, EventFlow uses a reactive offline inventory store.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
